put:
  operationId: uploadFilesMock
  summary: ローカル環境用Mock API
  tags:
    - File

  requestBody:
    required: true
    content:
      application/pdf:
        schema:
          type: string
          format: binary

  responses:
    "200":
      description: 正常系
      content:
        application/pdf:
          schema:
            type: string
