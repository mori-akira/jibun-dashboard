get:
  operationId: getSalaryOcrTasksById
  summary: 給与OCRタスク取得(ID)
  description: IDを指定して給与OCRタスクを取得する
  tags:
    - Salary

  parameters:
    - in: path
      name: ocrTaskId
      description: OCRタスクID
      required: true
      schema:
        type: string
        format: uuid

  responses:
    "200":
      description: 正常時
      content:
        application/json:
          schema:
            $ref: "../../../openapi.yaml#/components/schemas/SalaryOcrTask"
    "400":
      $ref: "../../../openapi.yaml#/components/responses/BadRequest"
