get:
  summary: 給与データ取得
  tags:
    - Salary
  responses:
    "200":
      description: 正常時
      content:
        application/json:
          schema:
            $ref: "../../openapi.yaml#/components/schemas/Salary"
