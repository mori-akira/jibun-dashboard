type: object
description: 給与明細情報

required:
  - key
  - data

properties:

  key:
    description: 明細カテゴリキー
    type: string
  
  data:
    description: 明細カテゴリ
    type: array
    items:

      type: object

      required:
        - key
        - data
      
      properties:

        key:
          description: 明細キー
          type: string

        data:
          description: 明細情報
          type: number
          format: double
