type: object
description: 給与基本情報

required:
  - targetDate
  - overview
  - structure
  - payslipData

properties:
  targetDate:
    description: 対象年月日
    type: string
    format: date

  overview:
    $ref: overview.yaml

  structure:
    $ref: structure.yaml

  payslipData:
    type: array
    items:
      $ref: payslipData.yaml
