type: object
description: ボキャブラリー情報
allOf:
  - $ref: vocabularyId.yaml
  - $ref: vocabularyBase.yaml
  - type: object
    properties:
      tags:
        description: タグ
        type: array
        maxItems: 64
        uniqueItems: true
        items:
          $ref: vocabularyTag/vocabularyTag.yaml

      createdDateTime:
        description: 作成日時
        type: string
        format: date-time

      updatedDateTime:
        description: 更新日時
        type: string
        format: date-time
