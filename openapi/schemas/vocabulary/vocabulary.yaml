type: object
description: ボキャブラリー

required:
  - name

properties:
  vocabularyId:
    description: ボキャブラリーID
    type: string
    format: uuid

  userId:
    description: ユーザID
    type: string

  name:
    description: 名前
    type: string
    maxLength: 100

  description:
    description: 説明
    type: string
    maxLength: 2000
  
  tags:
    description: タグ
    type: array
    maxItems: 50
    uniqueItems: true
    items:
      type: string
      maxLength: 50
