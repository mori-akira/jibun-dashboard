type: object
description: カード帳クイズ回答
required:
  - cardId
  - result
properties:
  cardId:
    description: カードID
    type: string
    format: uuid
  result:
    description: 回答結果 (正答 / 誤答)
    type: string
    enum:
      - CORRECT
      - INCORRECT
