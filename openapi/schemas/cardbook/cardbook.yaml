type: object
description: カード帳情報
allOf:
  - $ref: cardbookId.yaml
  - $ref: cardbookBase.yaml
  - type: object
    properties:
      createdDateTime:
        description: 作成日時
        type: string
        format: date-time

      updatedDateTime:
        description: 更新日時
        type: string
        format: date-time
